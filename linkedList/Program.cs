using System;

public class Program
{
    public static void Main()
    {
        LinkedList<int> lista = new LinkedList<int>();
        lista.Add(32);
        lista.Add(98);
        lista.Add(23);

        lista.Add(54);
        // lista.Percorre();
        lista.listaLinkada();
    }
}
